rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Public read access for portfolio media (profile photos, project images)
    // Write access restricted to authenticated admin (request.auth != null)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
